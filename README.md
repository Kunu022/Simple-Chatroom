a simple local chatroom with rust backed and a C++ QT frontend and ui.

the chatroom_ui is the C++ file for compiling the needed lib and Rusted the main code. 
to run make sure the C++ dll is built and were rust can use it (both lib folder and next to its exe), then run the rust server and any number of clients from there
